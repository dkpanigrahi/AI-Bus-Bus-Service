package com.demo.enums;

/**
 * Whether this bus has one deck or two (upper + lower berths).
 */
public enum DeckType {
    /** Normal seater / regular bus — only one level */
    SINGLE_DECK,
    /** Sleeper bus with upper and lower berths */
    DOUBLE_DECK
}